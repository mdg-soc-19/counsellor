<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.counsellor">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".answer"></activity>
        <activity android:name=".Research" />
        <activity android:name=".PnI" />
        <activity android:name=".ask" />
        <activity android:name=".Login_form" />
        <activity android:name=".Signup" />
        <activity android:name=".finance" />
        <activity android:name=".travel" />
        <activity android:name=".pg_exams" />
        <activity android:name=".litr" />
        <activity android:name=".campus_groups" />
        <activity android:name=".tv_series" />
        <activity android:name=".comp_coding" />
        <activity
            android:name=".ui.login.Profile_Login"
            android:label="@string/title_activity_profile__login" />
        <activity android:name=".notification" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>